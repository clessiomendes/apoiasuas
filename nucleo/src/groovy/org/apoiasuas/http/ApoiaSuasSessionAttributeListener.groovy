package org.apoiasuas.http

import groovy.util.logging.Log4j
import org.apache.log4j.Logger

import javax.servlet.http.HttpSessionAttributeListener
import javax.servlet.http.HttpSessionBindingEvent

@Log4j
class ApoiaSuasSessionAttributeListener {
/*  implements HttpSessionAttributeListener {

    @Override
    public void attributeAdded(HttpSessionBindingEvent event) {
        log.debug("Objeto adicionada à sessão: ${event.name} (${event.value})")
//        System.out.println("Objeto adicionada à sessão : ${event.name} (${event.value})")
    }

    @Override
    public void attributeRemoved(HttpSessionBindingEvent event) {
        log.debug("Objeto removido da sessão: ${event.name} (${event.value})")
//        System.out.println("Objeto removido da sessão: ${event.name} (${event.value})")
    }

    @Override
    public void attributeReplaced(HttpSessionBindingEvent event) {
        log.debug("Objeto substituido na sessão: ${event.name} (${event.value})")
//        Logger.getLogger("sessionatributes").fatal("Objeto substituido na sessão: ${event.name} (${event.value})")
//        System.out.println("Objeto substituido na sessão: ${event.name} (${event.value})")
    }
*/
}
package org.apoiasuas.report;

import javax.servlet.ServletConfig;
import javax.servlet.ServletException;

public class ReportServlet extends fr.opensagres.xdocreport.document.web.dispatcher.ProcessDispatcherXDocReportServlet {
    @Override
    public void init( ServletConfig config ) throws ServletException {
        super.init(config);
        registerDispatcher(new ReportDispatcher());
    }
}
